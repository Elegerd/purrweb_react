import React, {useEffect, useRef, useState} from "react";
import PropTypes from "prop-types";
import "./textareagroup.css"


const Comment = ({ }) => {

    return (
        <div ref={} className={'comment'}>

        </div>
    );
};


Comment.propTypes = {
}

Comment.defaultProps = {

}

export default Comment;